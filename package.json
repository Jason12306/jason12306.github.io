{
  "name": "vitepress-blog",
  "version": "1.0.0",
  "description": "vitepress blog",
  "main": "index.js",
  "scripts": {
    "docs:dev": "cross-env NODE_ENV=development vitepress dev docs",
    "docs:build": "cross-env NODE_ENV=production vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "publish": "npm run docs:build && node nodejs/publish.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "blog",
    "vue",
    "vite",
    "vitepress"
  ],
  "author": "vanyi",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "^1.0.0-alpha.20",
    "vue": "^3.2.40"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search"
      ]
    }
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
